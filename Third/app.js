/*Create a function to accept a string and return the string with all 
characters in small. */

  let lowerCaseString = str => str.toLowerCase();
  let test = lowerCaseString("LOWERCASE");
console.log(test);